# Phase 13A Step 2M-A5-PRE1 — Plan

1. Freeze the deterministic fixture, synthetic compatibility provenance, one-output topology, and construction-only boundaries before production work.
2. Add one public fixture builder that creates ten fixed synthetic candidates and complete one-input/one-output metadata, then delegates geometry exclusively to `construct_recipe_selections()` and the existing trade-up engine.
3. Wrap the returned solver selection in existing public live classification/construction DTOs using an explicit retained listing-ID-to-source-token map and deterministic compatibility tokens.
4. Add offline behavioral and AST tests proving exact one/ten/one geometry, deterministic provenance/configuration, real solver/engine derivation, zero output placeholders, and absence of provider/valuation/runtime behavior.
5. Document the synthetic, non-buyable, non-queryability, future-one-request meaning and validate all focused/full checks offline.
6. Leave all work unstaged and uncommitted; do not run a live smoke or begin A5-PRE2.
