# Phase 13A Step 2M-A5 — Plan

1. Freeze the standalone A5 gate, public CLI, guard order, request budget, output, failure, lifecycle, and synthetic-topology contracts before runtime implementation.
2. Add one independent disabled environment gate and a standalone smoke runtime that owns one HTTPX client and one zero-retry SteamDT client.
3. Build the zero-argument verified fixture, reject invalid one-recipe/ten-input/one-output geometry before runtime construction, and delegate exactly once to the existing closed SteamDT BUFF live-recipe valuation composition.
4. Validate only final valuation structure and risk-pass state; keep negative risk decisions successful and never print names, prices, economics, IDs, seeds, raw provider data, credentials, headers, or URLs.
5. Add offline guard, local-transport, full-composition, rejection, lifecycle, redaction, process-control, entrypoint, and architecture tests covering the complete requested matrix without changing protected core.
6. Document that the real component is only one aggregate output-price lookup while all recipe inputs and topology remain synthetic.
7. Run all required focused and full offline validation, record actual results, audit exactly seven unstaged paths and an empty index, check inherited gate/key presence without values, and stop without executing the live smoke, committing, or pushing.
