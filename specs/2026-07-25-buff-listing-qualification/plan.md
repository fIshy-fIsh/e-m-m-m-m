# Phase 12E3B — Plan

1. Define the three derived qualification outcomes, fixed safe validation error, evaluator callable protocol, immutable result, exact-type checks, and defensive snapshot invariants.
2. Implement the narrow qualification service with one facts lookup, zero-or-one eligibility evaluation, exact identity/result correlation, and unchanged collaborator error propagation.
3. Add focused tests for normal outcomes, missing semantics, call counts, snapshot detachment, tampered collaborators, redaction, exception propagation, and architecture boundaries.
4. Document the isolated E3B composition seam, missing-not-rejected behavior, existing facts/rules authority, and lack of runtime or external integration.
5. Run focused/full validation, three unchanged offline dry-runs, and an exact seven-file scope/security audit without staging, committing, or pushing.
