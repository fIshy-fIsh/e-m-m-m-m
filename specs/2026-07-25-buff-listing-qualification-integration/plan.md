# Phase 12E4A — Plan

1. Define the manual offline integration contract, approved file scope, failure behavior, and confidentiality boundary.
2. Add dedicated project-owned synthetic listing and facts fixtures covering qualified, rejected, duplicate, and missing-facts outcomes.
3. Implement a side-effect-free command that reuses the strict loaders, normalizer, offline facts provider, default eligibility policy, and qualification service in the required order.
4. Add focused tests for the full chain, immutable run result, call counts/order, CLI entrypoints, safe output, error exits, and architecture isolation.
5. Document the offline milestone and its production limitations in README and BUFF listing notes.
6. Run the complete requested offline validation and exact-scope audit; do not commit, push, update the roadmap, or begin another phase.
