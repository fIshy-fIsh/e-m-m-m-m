# Phase 13B Step 2B — Plan

1. Freeze the user-authorized empirical endpoint/query, anonymous request boundary, first-item schema, failure allowlist, output redaction, one-request lifecycle, and seven-path scope.
2. Add an independent disabled gate and explicit nonsecret goods-ID environment variable.
3. Implement one standalone owned HTTPX runtime with a fixed transparent User-Agent, disabled redirects/environment proxies, exact request-contract guard, and a hard one-transport-attempt ceiling.
4. Decode one response strictly, inspect only `data.items[0]`, and validate only sell-order ID presence, positive Decimal price, bounded paintwear, and optional asset/seed presence.
5. Add offline fake/local-transport tests for all 50 required behaviors, including no-auth/no-evasion static guards and process-control cleanup.
6. Document that this is an unofficial research-only anonymous compatibility probe, not OpenAPI, a production provider, or a transaction capability.
7. Run all requested offline validation, audit exactly seven unstaged paths and an empty index, check inherited gate/goods-ID presence without values, and stop without running the live smoke, committing, or pushing.
