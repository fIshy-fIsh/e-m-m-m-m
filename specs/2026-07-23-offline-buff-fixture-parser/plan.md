# Phase 12E2A Plan

1. Define a project-owned, versioned offline BUFF listing fixture contract without claiming an official API mapping.
2. Add a strict mapping parser and thin JSON file loader with safe classified errors.
3. Reuse `BuffListingObservation` validation for domain rules and preserve Decimal precision, UTC time, order, duplicates, and immutability.
4. Add a clearly synthetic v1 fixture plus focused valid-path, strict-schema, safety, and architecture-boundary tests.
5. Document the fixture/parser boundary and retain all live BUFF transport details as unconfirmed TODOs.
6. Run the complete offline validation matrix and audit scope, secrets, generated files, and Git state without committing or pushing.
