# Phase 13A Step 2D — Plan

1. Freeze the exact catalog, classification, bucket, rejection, redaction, ordering, and scope contracts.
2. Add a detached exact-name `SkinMetadataCatalog` with global duplicate-name rejection and immutable solver-mode indexes.
3. Classify one immutable SteamApis pool snapshot through the unchanged Step 2B adapter into eligible bindings or typed rejections.
4. Build deterministic `(input_rarity, stattrak, souvenir)` buckets while preserving mixed collections and affected collection sets.
5. Add focused behavioral, immutability, redaction, propagation, and architecture tests.
6. Document the offline Step 2D boundary, validate every required command, and stop unstaged, uncommitted, and unpushed before Step 2E.
