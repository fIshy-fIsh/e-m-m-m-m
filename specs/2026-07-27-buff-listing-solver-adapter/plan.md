# Phase 12E4B — Plan

1. Add the isolated single-record adapter and one fixed safe public error.
2. Require the exact qualification-result type, rebuild it through its existing public constructor, and apply explicit qualified/facts/decision/goods-ID/float/quantity gates.
3. Map the detached qualified candidate into the existing `CandidateListing`, preserving Decimal price and converting Decimal float once at the legacy boundary.
4. Add focused mapping, fail-closed, exception-boundary, determinism, import-isolation, and reverse-wiring tests.
5. Update README and BUFF listing notes without claiming solver execution or runtime integration.
6. Run focused and full tests, Ruff, Mypy, all three existing dry-runs, whitespace checks, and exact scope/security audits.
7. Leave all work uncommitted and unpushed; do not begin batch or runtime integration.
