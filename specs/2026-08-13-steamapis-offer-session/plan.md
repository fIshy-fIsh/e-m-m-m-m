# Phase 13A Step 2I — Plan

1. Freeze the exact session-runner API, consumed-count definition, safe error boundary, and no-rollback semantics.
2. Implement one foreground sequential loop from the existing WebSocket observation iterator into the existing offer pool.
3. Add focused contract, sequencing, failure, propagation, retention, and real-client offline integration tests.
4. Document the authority split, normal/failure behavior, exclusions, and unchanged SteamDT currency blocker.
5. Run focused regressions, the full quality gates, offline dry-runs, and exact-scope/security audits.
6. Leave the work unstaged, uncommitted, and unpushed; do not resume Step 2G or begin Step 2J.
